@model IEnumerable<CNW_WebBanQuanAo.Models.SANPHAM>

@{
    ViewBag.Title = "Index";
}

<h2>Danh sách chi tiết sản phẩm</h2>

<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MATHANG.TenMH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MAU.TenMau)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SIZE.MaSize)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoLuong)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MATHANG.TenMH)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MAU.TenMau)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SIZE.MaSize)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoLuong)
        </td>
        <td>
            @Html.ActionLink("Chỉnh sửa", "Edit", new { id=item.MaQA }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.MaQA }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaQA })
        </td>
    </tr>
}

</table>
